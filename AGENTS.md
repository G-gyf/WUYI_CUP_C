# AGENTS.md

## 项目说明

本项目对应“2026年第二十三届五一数学建模竞赛 C题：边坡预警问题”。

赛题文件：

```text
2026-51MCM-Problem C.docx
```

附件目录：

```text
C 附件(Attachment)
```

本文件仅对赛题结构、数据附件、任务要求和必须提交的结果进行拆解，不预设具体建模路线。

## 赛题背景

边坡稳定性是水利工程、交通路网、露天矿山等领域的重要安全问题。边坡受地质条件、气象变化、工程扰动等多因素影响，可能发生崩塌、滑坡等地质灾害。

题目给出多源监测数据，涉及表面位移、深部位移、降雨量、孔隙水压力、微震事件数、爆破信息、干湿入渗系数等变量。需要围绕边坡形变识别、表面位移预测和预警机制完成五个问题。

题目强调边坡破坏前通常存在“三段式形变”：

1. 缓慢匀速形变阶段：位移速度基本恒定，坡体缓慢稳定调整。
2. 加速形变阶段：位移速度显著增大，坡体进入非稳定形变阶段。
3. 快速形变阶段：位移速度急剧增大，坡体趋近整体失稳破坏。

题目同时指出，多源监测数据中可能存在强噪声、异常跳变、设备故障造成的数据缺失，以及工程爆破、气候变化、电磁干扰等环境因素影响。

## 附件总览

### 附件1：两组位移时序数据-问题1.xlsx

用途：问题1。

工作表：

```text
Sheet1
```

字段：

```text
时间
数据A_光纤位移计数据_mm
数据B_振弦式位移计数据_mm
```

数据说明：

```text
同一边坡、同一监测点的两组位移时序数据。
A：新型光纤位移计数据，存在传感器零漂、安装偏差等偏移。
B：传统振弦式位移计数据，经验证为基准参考数据。
采集频率：10 分钟一次。
题面说明时间序列从 2024-01-01 00:00 起。
```

### 附件2：位移时序数据-问题2.xlsx

用途：问题2。

工作表：

```text
Sheet1
```

字段：

```text
编号
表面位移_mm
```

数据说明：

```text
编号对应数据采集时刻。
采集起点：2024-05-04 00:00。
采集频率：10 分钟一次。
```

### 附件3：监测数据（训练集与实验集）-问题3.xlsx

用途：问题3。

工作表：

```text
训练集
实验集
```

训练集字段：

```text
编号
a:降雨量_mm
b:孔隙水压力_kPa
c:微震事件数
d:深部位移_mm
e:表面位移_mm
```

实验集字段：

```text
编号
降雨量_mm
孔隙水压力_kPa
微震事件数
深部位移_mm
表面位移_mm
```

数据说明：

```text
训练集包含 5 个监测变量。
实验集包含同类变量，但表面位移列为空，需要估计。
数据中存在异常值与缺失值。
缺失值在表中表现为未填写数据。
```

变量字母对应关系：

```text
a：降雨量
b：孔隙水压力
c：微震事件数
d：深部位移
e：表面位移
```

### 附件4：监测数据（训练集与实验集）-问题4.xlsx

用途：问题4。

工作表：

```text
训练集
实验集
```

训练集字段：

```text
时间
表面位移_mm
降雨量_mm
孔隙水压力_kPa
微震事件数
爆破点距离_m
单段最大药量_kg
```

实验集字段：

```text
时间
阶段标签
表面位移_mm
降雨量_mm
孔隙水压力_kPa
微震事件数
爆破点距离_m
单段最大药量_kg
```

数据说明：

```text
训练集包含表面位移和 5 项特征指标。
实验集不含表面位移，需要预测。
实验集包含阶段标签。
爆破为偶发事件。
非爆破时刻，爆破点距离和单段最大药量字段为空。
训练集与实验集均包含缓慢匀速形变、加速形变、快速形变三阶段演变。
实验集与训练集在对应阶段的表面位移变化规律一致。
训练集与实验集的阶段转换节点不一致。
```

### 附件5：监测数据-问题5.xlsx

用途：问题5。

工作表：

```text
Sheet1
```

字段：

```text
时间
表面位移_mm
降雨量_mm
孔隙水压力_kPa
微震事件数
干湿入渗系数
爆破点距离_m
单段最大药量_kg
```

数据说明：

```text
包含表面位移和 6 类候选解释变量。
爆破为偶发事件。
非爆破时刻，爆破点距离和单段最大药量字段为空。
```

## 问题拆解

### 问题1：数据 A 校正

输入数据：

```text
附件1中的数据 A 和数据 B。
```

题目设定：

```text
B 为传统振弦式位移计监测数据，经验证为基准参考数据。
A 为新型光纤位移计获取数据，但存在偏移。
```

需要完成：

1. 建立数学模型，对数据 A 进行校正。
2. 使校正后的结果与数据 B 的偏差尽可能小。
3. 采用交叉验证评估模型性能和效果。
4. 给出偏差量化指标。
5. 对题目给出的 5 个校正前数据进行验证。
6. 将校正结果填入表 1.1。

表 1.1 需要填写：

```text
校正前数据 x：
7.132
18.526
84.337
123.554
167.667

校正后数据 y：
需要计算并填写。
```

### 问题2：三段式形变阶段识别

输入数据：

```text
附件2中的表面位移时序数据。
```

题目设定：

```text
编号对应采集时刻。
采集起点为 2024-05-04 00:00。
采集频率为 10 分钟一次。
```

需要完成：

1. 识别三段式形变阶段的转换节点。
2. 识别“缓慢匀速形变 -> 加速形变”的转换节点。
3. 识别“加速形变 -> 快速形变”的转换节点。
4. 给出区分“噪声/工程扰动引起的瞬时跳变”和“真实阶段转换节点”的核心准则。
5. 根据转换节点对位移时序数据进行阶段划分。
6. 对各阶段分别建立数学模型。
7. 给出各阶段模型参数。
8. 对各阶段模型进行检验。
9. 计算各阶段表面位移平均速度。

阶段平均速度定义：

```text
阶段平均速度 = 该阶段表面位移变化总量 / 该阶段总持续时间
单位：mm/h
```

### 问题3：多变量监测数据处理、异常识别与表面位移估计

输入数据：

```text
附件3中的训练集与实验集。
```

变量对应关系：

```text
a：降雨量
b：孔隙水压力
c：微震事件数
d：深部位移
e：表面位移
```

#### 问题3.1：去噪与缺失值补齐

需要完成：

1. 对附件3训练集的各变量数据进行去噪。
2. 对附件3训练集的各变量缺失值进行补齐。
3. 详细阐述各处理步骤的数学依据。

涉及变量：

```text
降雨量
孔隙水压力
微震事件数
深部位移
表面位移
```

#### 问题3.2：异常值检测与共同异常点识别

需要完成：

1. 基于问题3.1预处理后的训练集数据，对各变量开展异常值检测。
2. 识别单变量异常点。
3. 识别共同异常点。
4. 共同异常点定义为：同一时间点有不少于 2 个监测变量同时异常。
5. 将单变量异常点检出结果填入表 3.1。
6. 将多变量共同异常点变量清单填入表 3.2。

表 3.1 需要填写：

```text
数据集变量
异常点数量

a：降雨量
b：孔隙水压力
c：微震事件数
d：深部位移
e：表面位移
总数
```

表 3.2 需要填写：

```text
时间点对应编号
共同异常点处的异常变量
```

填写格式示例：

```text
abc
```

其中字母对应表 3.1 中的变量。

#### 问题3.3：表面位移关联分析与实验集估计

需要完成：

1. 基于问题3.1预处理后的训练集建立数学模型。
2. 定量分析边坡表面位移与以下变量之间的关联：

```text
降雨量
深部位移
孔隙水压力
微震事件数
```

3. 评估各因素对表面位移变化的贡献程度。
4. 将所建立模型应用于附件3实验集。
5. 估计实验集对应的表面位移。
6. 绘制散点图。

### 问题4：含爆破因素的分阶段表面位移预测

输入数据：

```text
附件4中的训练集与实验集。
```

训练集包含：

```text
表面位移
降雨量
孔隙水压力
微震事件数
爆破点距离
单段最大药量
```

实验集包含：

```text
阶段标签
降雨量
孔隙水压力
微震事件数
爆破点距离
单段最大药量
```

实验集不包含：

```text
表面位移
```

#### 问题4.1：训练集分阶段模型

题目设定：

```text
训练集包含缓慢匀速形变、加速形变、快速形变三阶段演变。
不同阶段中，降雨、爆破、微震等外界扰动对位移的影响可能发生显著变化。
```

需要完成：

1. 根据以下 5 项特征指标建立分阶段数学模型：

```text
降雨量
孔隙水压力
微震事件数
爆破点距离
单段最大药量
```

2. 分析表面位移变化规律。
3. 预测表面位移变化规律。

#### 问题4.2：实验集表面位移预测

题目设定：

```text
实验集包含三阶段演变。
实验集阶段转换节点已在阶段标签中给出。
训练集与实验集阶段转换节点不一致。
实验集与训练集在对应阶段的表面位移变化规律一致。
```

需要完成：

1. 根据问题4.1构建的数学模型，对实验集进行表面位移预测。
2. 分析表面位移预测结果。
3. 绘制表面位移随时间变化的图像。
4. 根据预测结果补全表 4.1。

表 4.1 需要填写：

```text
时间点                         表面位移预测值
2025-05-09 12:00              待填写
2025-05-27 08:00              待填写
2025-06-01 12:00              待填写
2025-06-03 22:00              待填写
2025-06-04 01:40              待填写
```

### 问题5：变量组合建模与滑坡预警机制

输入数据：

```text
附件5。
```

附件5包含：

```text
表面位移
降雨量
孔隙水压力
微震事件数
干湿入渗系数
爆破点距离
单段最大药量
```

#### 问题5.1：5 类变量组合与误差评估

候选变量：

```text
降雨量
孔隙水压力
微震事件数
干湿入渗系数
爆破点距离
单段最大药量
```

需要完成：

1. 从上述 6 类候选变量中选取 5 类变量。
2. 构建数学模型，定量分析边坡表面位移。
3. 评估不同变量组合下的表面位移数据误差。
4. 给出最优变量组合模型。
5. 解释最优变量组合模型的原理。

#### 问题5.2：分阶段预警机制

题目设定：

```text
边坡介质差异会导致边坡阶段划分与预警阈值不同。
以表面位移速度作为预警指标。
```

需要完成：

1. 根据问题5.1建立的最优模型，分阶段分析数据变化规律。
2. 构建滑坡预警机制。
3. 给出表面位移速度达到何种阈值时发出预警。
4. 解释预警机制及阈值设置的合理性。

## 题目名词解释

### 表面位移

通过布设于边坡表面，如坡顶、坡中各级台阶的高精度定位监测设备，获取的监测点在地理空间中的三维坐标变化量，单位为毫米。

### 深部位移

通过在边坡内部竖直钻孔，并在孔内植入分布式传感光缆，获取从孔口到孔底不同深度处岩土体相对于稳定基岩的水平剪切位移量，单位为毫米。

### 孔隙水压力

边坡岩土体孔隙中地下水产生的压力。降雨入渗会导致孔隙水压力升高，减小岩土体颗粒间有效接触应力，降低坡体抗滑能力。

### 微震事件数

边坡内部岩体在应力作用下发生微小破裂时，被微震监测系统捕获并自动定位的事件数量。该变量用于评估岩体内部损伤演化和破裂集聚过程。

### 累计降雨量

指定时间段内降雨量总和，单位为毫米。

### 位移速度

单位时间内边坡表面某点水平位移的变化量，单位为毫米每小时。

### 数据跳变

位移时序数据中某个短时间段内发生的、数值远超正常蠕变趋势范围的瞬时阶跃或尖峰变化。

### 干湿入渗系数

描述边坡土体在干湿交替过程中，非饱和渗透系数随含水率或干湿路径变化的参数，反映土体入渗能力随时间和气候的动态演变。

## 最终结果清单

项目后续求解时，需要围绕以下结果组织论文和附件：

```text
问题1：数据 A 校正模型、交叉验证、偏差量化指标、表1.1。
问题2：两个阶段转换节点、三阶段划分、各阶段模型参数和检验、各阶段平均速度。
问题3.1：训练集去噪与缺失补齐结果、数学依据。
问题3.2：表3.1 单变量异常点数量、表3.2 共同异常点变量清单。
问题3.3：关联分析、因素贡献程度、实验集表面位移估计、散点图。
问题4.1：训练集分阶段模型、表面位移变化规律分析与预测。
问题4.2：实验集表面位移预测、预测结果分析、位移时间图、表4.1。
问题5.1：5 类变量组合模型、不同组合误差、最优变量组合及原理解释。
问题5.2：分阶段数据规律、表面位移速度预警阈值、滑坡预警机制及合理性解释。
```

## 已完成工作记录：第一轮保守预处理

### 处理状态

已完成第一轮数据预处理，处理原则为“保守、可追溯、少改动”。本轮预处理不对原始附件进行修改，不进行平滑去噪，不进行缺失值插补，不删除或替换异常点，仅生成统一标记、质量日志、诊断图和处理后副本。

执行脚本：

```text
preprocess_conservative.py
```

输出目录：

```text
preprocess_outputs
```

### 预处理口径

本轮预处理采用以下统一口径：

1. 原始观测列全部保留，不覆盖原始数据。
2. 缺失值仅生成缺失标记和缺失日志，不进行插补。
3. 异常值仅生成候选标记，不删除、不截尾、不替换。
4. 附件3实验集和附件4实验集中的表面位移列作为待预测目标，保持全空，不纳入普通缺失修正。
5. 附件4和附件5中爆破点距离、单段最大药量的大量空值表示“非爆破时刻”，不作为普通缺失值处理。
6. 对爆破变量新增“是否爆破”和“爆破字段冲突标记”，用于后续分阶段预测和变量组合建模。
7. 对位移序列计算原始增量、原始速度、原始加速度等诊断列，仅用于后续阶段识别和预警分析的前置观察。
8. 对突变候选点采用稳健差分异常得分进行标记，当前口径为各序列差分异常得分最高约 0.5% 的明显候选点。

### 输出文件结构

处理后数据文件：

```text
preprocess_outputs/data/附件1_保守预处理.xlsx
preprocess_outputs/data/附件2_保守预处理.xlsx
preprocess_outputs/data/附件3_保守预处理.xlsx
preprocess_outputs/data/附件4_保守预处理.xlsx
preprocess_outputs/data/附件5_保守预处理.xlsx
```

质量日志文件：

```text
preprocess_outputs/logs/附件1_质量日志.csv
preprocess_outputs/logs/附件2_质量日志.csv
preprocess_outputs/logs/附件3_质量日志.csv
preprocess_outputs/logs/附件4_质量日志.csv
preprocess_outputs/logs/附件5_质量日志.csv
```

字段汇总文件：

```text
preprocess_outputs/summary/预处理字段汇总.csv
preprocess_outputs/summary/预处理字段汇总.xlsx
```

诊断图目录：

```text
preprocess_outputs/figures
```

当前共生成 18 张诊断图，包括 A-B 散点图、位移时序图、差值图、缺失统计图、速度与加速度图、爆破事件图和候选变量时序图。

### 各附件处理内容

#### 附件1

处理内容：

1. 解析时间字段并检查 10 分钟采样间隔。
2. 保留数据 A、数据 B 原始列。
3. 新增 A-B 差值和相对偏差诊断列。
4. 标记非起始位置零值和局部差分突变候选点。
5. 输出 A/B 时序图、A-B 差值图和 A-B 散点图。

日志结果：

```text
附件1_质量日志.csv：122 条
其中：局部差分突变候选 100 条，非起始位置零值 22 条
```

#### 附件2

处理内容：

1. 检查编号连续性。
2. 根据题意生成推算时间列：2024-05-04 00:00 起，每 10 分钟一个记录。
3. 新增原始增量、原始速度和原始加速度诊断列。
4. 标记非起始位置零值和局部差分突变候选点。
5. 输出位移、速度、加速度诊断图。

日志结果：

```text
附件2_质量日志.csv：106 条
其中：局部差分突变候选 100 条，非起始位置零值 6 条
```

#### 附件3

处理内容：

1. 统一训练集与实验集变量命名。
2. 将训练集变量统一为：

```text
a_降雨量_mm
b_孔隙水压力_kPa
c_微震事件数
d_深部位移_mm
e_表面位移_mm
```

3. 对训练集和实验集解释变量生成缺失标记。
4. 实验集 `e_表面位移_mm` 保持全空，作为后续预测目标。
5. 对微震事件数检查非负整数约束。
6. 对孔隙水压力、深部位移和表面位移标记局部差分突变候选点。
7. 输出训练集和实验集缺失统计图、训练集变量时序图、深部位移与表面位移散点图。

日志结果：

```text
附件3_质量日志.csv：3699 条
其中：缺失 3516 条，局部差分突变候选 183 条
```

关键核验：

```text
附件3实验集 e_表面位移_mm 缺失：5000 / 5000
```

#### 附件4

处理内容：

1. 解析训练集与实验集时间字段并检查 10 分钟采样间隔。
2. 保留训练集表面位移原始列。
3. 实验集表面位移保持全空，作为后续预测目标。
4. 新增 `是否爆破` 和 `爆破字段冲突标记`。
5. 对训练集表面位移新增原始增量、原始速度和原始加速度诊断列。
6. 对训练集表面位移和孔隙水压力标记局部差分突变候选点。
7. 输出训练集表面位移图、实验集阶段标签图、训练集和实验集爆破事件图。

日志结果：

```text
附件4_质量日志.csv：100 条
其中：局部差分突变候选 100 条
```

关键核验：

```text
附件4训练集是否爆破记录：41 条
附件4实验集是否爆破记录：24 条
爆破字段冲突记录：0 条
附件4实验集表面位移缺失：5000 / 5000
```

#### 附件5

处理内容：

1. 解析时间字段并检查 10 分钟采样间隔。
2. 保留表面位移、降雨量、孔隙水压力、微震事件数、干湿入渗系数和爆破变量原始列。
3. 新增 `是否爆破` 和 `爆破字段冲突标记`。
4. 新增表面位移原始增量、原始速度和原始加速度诊断列。
5. 标记非起始位置零值和局部差分突变候选点。
6. 输出表面位移图、原始速度图、爆破事件图和候选变量时序图。

日志结果：

```text
附件5_质量日志.csv：146 条
其中：局部差分突变候选 144 条，非起始位置零值 2 条
```

关键核验：

```text
附件5是否爆破记录：47 条
爆破字段冲突记录：0 条
```

### 后续使用说明

后续建模时应优先读取 `preprocess_outputs/data` 中的保守预处理数据，而不是直接覆盖或修改原始附件。缺失值插补、平滑去噪、异常点替换或删除均尚未实施，需要在进入具体问题求解前另行确定口径。

建议后续任务顺序：

1. 基于附件3质量日志确定问题3.1的缺失值补齐方法。
2. 基于附件2诊断图和速度列确定问题2阶段转换节点识别方法。
3. 基于附件4爆破标记和阶段标签口径建立问题4分阶段预测数据集。
4. 基于附件5速度诊断列和爆破标记开展问题5变量组合与预警阈值建模。

## 已完成工作记录：问题一数据 A 校正

### 执行状态

问题一已完成建模与结果输出。原始附件未修改，所有结果统一输出至：

```text
problem1_outputs
```

执行脚本：

```text
problem1_solution.py
```

### 建模口径

附件1无缺失记录，时间间隔为 10 分钟。问题一目标为建立新型光纤位移计数据 A 到传统振弦式位移计基准数据 B 的校正映射，因此未对原始序列进行平滑或插值处理。

采用 Huber 稳健一元仿射校正模型。初始零点保留，非初始时刻出现的零值记录作为疑似异常记录标记并排除模型参数估计。最终有效建模样本量为 9981 条，排除非初始零值记录 19 条。

校正方程为：

```text
y = -0.533095257 + 0.881763677 * x
```

其中 `x` 为校正前数据 A，`y` 为校正后数据。

### 主要结果

表 1.1 校正结果：

| 校正前数据 x | 校正后数据 y |
|---:|---:|
| 7.132 | 5.756 |
| 18.526 | 15.802 |
| 84.337 | 73.832 |
| 123.554 | 108.412 |
| 167.667 | 147.310 |

校正前后误差指标：

```text
校正前全部样本 RMSE：26.795478 mm
校正后全部样本 RMSE： 2.685069 mm
校正前全部样本 MAE ：23.336679 mm
校正后全部样本 MAE ： 1.289217 mm
5折时间分块交叉验证 RMSE：2.682091 mm
5折时间分块交叉验证 MAE ：1.285625 mm
5折时间分块交叉验证 R2  ：0.999221
```

### 输出路径

```text
problem1_outputs/data/问题1_校正结果.xlsx
problem1_outputs/data/问题1_校正明细.csv
problem1_outputs/tables/表1_1_校正结果.csv
problem1_outputs/tables/问题1_校正前后误差指标.csv
problem1_outputs/tables/问题1_时间分块交叉验证.csv
problem1_outputs/tables/问题1_模型对照指标.csv
problem1_outputs/logs/问题1_建模日志.txt
problem1_outputs/logs/问题1_建模日志.json
problem1_outputs/logs/问题1_非初始零值异常日志.csv
problem1_outputs/figures/问题1_A_B散点与校正曲线.png
problem1_outputs/figures/问题1_校正前后误差对比.png
problem1_outputs/figures/问题1_校正前后残差箱线图.png
```

## 已完成工作记录：问题二三段式形变阶段识别

### 执行状态

问题二已完成建模与结果输出。原始附件未修改，所有结果统一输出至：

```text
problem2_outputs
```

执行脚本：

```text
problem2_solution.py
```

### 建模口径

附件2共 10000 条记录，编号连续，无缺失。根据题意将编号换算为时间，起点为 `2024-05-04 00:00:00`，采样间隔为 10 分钟，终点为 `2024-07-12 10:30:00`。

采用“稳健平滑 + 三段分段回归 + 速度持续性诊断”的阶段识别口径。原始位移不插值、不替换；6 个非初始零值和局部差分突变仅作异常候选标记。稳健平滑仅用于阶段识别和速度诊断，不替代原始位移数据。阶段平均速度按题目定义由原始位移端点和实际持续时间计算。

### 主要结果

阶段转换节点：

| 转换类型 | 转换点序号 | 转换点时间 | 原始位移/mm |
|---|---:|---|---:|
| 缓慢匀速形变阶段 -> 加速形变阶段 | 5275 | 2024-06-09 15:00:00 | 154.397 |
| 加速形变阶段 -> 快速形变阶段 | 8867 | 2024-07-04 13:40:00 | 522.731 |

阶段平均速度：

| 阶段 | 序号范围 | 时间范围 | 平均速度/(mm/h) |
|---|---|---|---:|
| 缓慢匀速形变阶段 | 1-5274 | 2024-05-04 00:00 至 2024-06-09 14:50 | 0.175499 |
| 加速形变阶段 | 5275-8866 | 2024-06-09 15:00 至 2024-07-04 13:30 | 0.609724 |
| 快速形变阶段 | 8867-10000 | 2024-07-04 13:40 至 2024-07-12 10:30 | 4.138157 |

分段数对照：

```text
单段线性趋势 SSE：1.776044e+08
两段线性趋势 SSE：1.314556e+07
三段线性趋势 SSE：5.296594e+06
三段相对单段 SSE 下降率：0.970178
```

### 输出路径

```text
problem2_outputs/data/问题2_阶段划分结果.xlsx
problem2_outputs/data/问题2_阶段划分明细.csv
problem2_outputs/tables/问题2_阶段转换节点.csv
problem2_outputs/tables/问题2_分阶段模型参数.csv
problem2_outputs/tables/问题2_阶段平均速度.csv
problem2_outputs/tables/问题2_模型检验指标.csv
problem2_outputs/tables/问题2_分段数对照.csv
problem2_outputs/tables/问题2_平滑窗口敏感性.csv
problem2_outputs/logs/问题2_建模日志.txt
problem2_outputs/logs/问题2_建模日志.json
problem2_outputs/logs/问题2_非初始零值日志.csv
problem2_outputs/logs/问题2_局部差分突变候选日志.csv
problem2_outputs/figures/问题2_位移阶段划分图.png
problem2_outputs/figures/问题2_速度加速度诊断图.png
problem2_outputs/figures/问题2_分阶段拟合图.png
```

## 已完成工作记录：问题三多变量预处理、异常识别与表面位移估计

### 执行状态

问题三已完成建模与结果输出。原始附件未修改，基于第一轮保守预处理后的附件3数据开展问题3.1、问题3.2和问题3.3。所有结果统一输出至：

```text
problem3_outputs
```

执行脚本：

```text
problem3_solution.py
```

### 建模口径

问题三采用“分变量预处理、稳健异常识别、可解释回归预测”的统一口径。训练集共 10000 条记录，实验集共 5000 条记录；实验集表面位移列作为待预测目标，不参与缺失补齐。

问题3.1中，降雨量按事件型变量处理，保留降雨峰值；微震事件数按非负离散计数变量处理；孔隙水压力、深部位移和表面位移按连续监测变量处理。所有变量均保留原始列，并新增缺失标记、补齐值和建模值。

问题3.2中，异常检测采用稳健统计与前置突变候选标记相结合的方式。共同异常点定义为同一编号下不少于 2 个变量同时异常。表3.1中“总数”为五个变量异常次数之和，不是异常编号去重数。

为补充异常检测准则的选择依据，已对五个变量的建模序列进行分布诊断，输出直方图、偏度、超额峰度、高分位数和方差均值比。诊断结果显示，降雨量偏度约为 3.3319、超额峰度约为 12.9642，具有明显右偏和尖峰特征，不宜采用正态 3σ 作为主判据；微震事件数为非负整数计数变量，采用泊松分布仅作为离散性诊断参考，主判据采用百分位数与局部突增规则；孔隙水压力、深部位移和表面位移虽可将 3σ 作为对照，但主异常识别仍采用 MAD 稳健统计与局部趋势残差方法。

本轮异常检测阈值口径为：连续监测变量采用趋势窗口 11 点、局部 MAD 窗口 31 点；降雨量不设置主滑动窗口，仅使用高分位孤立峰值与前后邻点持续性约束；微震事件数采用 11 点局部中位数窗口、非负整数约束和 P99.5 高分位阈值。

问题3.3中，以表面位移为响应变量，以降雨量、孔隙水压力、微震事件数和深部位移为解释变量。模型比较包括普通线性回归、岭回归、Huber稳健回归和随机森林辅助对照。主模型采用岭回归，未纳入滞后项。

### 主要结果

训练集缺失记录数：

| 变量 | 缺失数 |
|---|---:|
| a：降雨量 | 510 |
| b：孔隙水压力 | 475 |
| c：微震事件数 | 510 |
| d：深部位移 | 456 |
| e：表面位移 | 559 |

表3.1异常点数量：

| 数据集变量 | 异常点数量 |
|---|---:|
| a：降雨量 | 10 |
| b：孔隙水压力 | 46 |
| c：微震事件数 | 0 |
| d：深部位移 | 46 |
| e：表面位移 | 59 |
| 总数 | 161 |

表3.2共同异常点数量：

```text
共同异常点数量：8
异常编号去重数：152
```

模型检验结果：

| 模型 | RMSE/mm | MAE/mm | R2 |
|---|---:|---:|---:|
| 普通线性回归 | 4.157182 | 2.306602 | 0.912123 |
| 岭回归 | 4.157290 | 2.310095 | 0.912119 |
| Huber稳健回归 | 4.205889 | 2.222492 | 0.910052 |
| 随机森林辅助对照 | 4.240573 | 2.589882 | 0.908563 |

实验集表面位移估计结果：

```text
预测记录数：5000
预测最小值：58.571535 mm
预测平均值：94.160616 mm
预测最大值：162.329583 mm
```

### 输出路径

```text
problem3_outputs/data/问题3_训练集预处理结果.xlsx
problem3_outputs/data/问题3_训练集预处理结果.csv
problem3_outputs/data/问题3_实验集特征预处理结果.xlsx
problem3_outputs/data/问题3_实验集特征预处理结果.csv
problem3_outputs/tables/表3_1_单变量异常点数量.csv
problem3_outputs/tables/表3_2_共同异常点清单.csv
problem3_outputs/tables/问题3_关联分析指标.csv
problem3_outputs/tables/问题3_变量贡献度.csv
problem3_outputs/tables/问题3_变量分布统计.csv
problem3_outputs/tables/问题3_异常判据说明.csv
problem3_outputs/tables/问题3_模型检验指标.csv
problem3_outputs/tables/问题3_时间分块交叉验证.csv
problem3_outputs/tables/问题3_滞后相关性观察.csv
problem3_outputs/tables/问题3_实验集表面位移估计.csv
problem3_outputs/tables/问题3_交叉验证预测明细.csv
problem3_outputs/logs/问题3_处理日志.txt
problem3_outputs/logs/问题3_处理日志.json
problem3_outputs/figures/问题3_训练集缺失统计.png
problem3_outputs/figures/问题3_变量分布直方图.png
problem3_outputs/figures/问题3_解释变量与表面位移散点图.png
problem3_outputs/figures/问题3_实验集表面位移估计序列.png
problem3_outputs/figures/问题3_真实值预测值散点图.png
problem3_outputs/figures/问题3_残差随编号变化图.png
problem3_outputs/figures/问题3_残差分布图.png
```
## 已完成工作记录：问题四含爆破因素的分阶段表面位移预测

### 执行状态

问题四已完成建模、实验集预测、表4.1结果提取、图件和日志输出。原始附件未修改，所有结果统一输出至：

```text
problem4_outputs
```

执行脚本：

```text
problem4_solution.py
```

### 建模口径

训练集未给出阶段标签，因此先基于训练集表面位移序列识别三阶段转换节点；实验集已给出阶段标签，预测时直接使用实验集阶段标签，不重新识别实验集阶段。

模型采用“阶段趋势项 + 趋势外残差扰动项”的分解结构。阶段趋势项刻画对应阶段内的基本位移增长规律，趋势外残差扰动项采用分阶段岭回归描述降雨量、孔隙水压力、微震事件数和爆破因素对局部增量的修正作用。

爆破变量处理口径为：爆破点距离和单段最大药量非空表示爆破时刻，空值表示非爆破时刻，不作为普通缺失值插补。模型中构造了是否爆破、爆破强度和1小时衰减爆破强度。

实验集初始位移基准说明：由于附件4实验集表面位移列全空，无法由实验集自身确定绝对初始位移，当前采用训练集初始表面位移作为实验集预测序列的累计起点，并对累计位移施加非负下界。

外推风险检查说明：实验集第三阶段孔隙水压力存在明显超过训练集第三阶段范围的情况，其中孔隙水压力模型输入截尾记录为459条。为避免线性模型在训练数据不支持的范围内放大预测，当前仅对模型输入副本按训练集对应阶段范围进行截尾，原始观测列保持不变；同时对实验集各阶段残差增量进行中心化，使扰动项只修正局部形态，不改变对应阶段总体位移规律。

### 主要结果

训练集阶段转换节点：

```text
缓慢匀速形变 -> 加速形变：2023-06-15 05:20:00
加速形变 -> 快速形变：2023-07-02 03:40:00
```

训练集累加拟合检验：

```text
RMSE：4.293482 mm
MAE ：2.731851 mm
R2  ：0.998803
```

表4.1指定时刻预测结果：

| 时间点 | 表面位移预测值/mm | 阶段标签 |
|---|---:|---:|
| 2025-05-09 12:00 | 0.000 | 1 |
| 2025-05-27 08:00 | 22.009 | 2 |
| 2025-06-01 12:00 | 41.545 | 2 |
| 2025-06-03 22:00 | 210.151 | 3 |
| 2025-06-04 01:40 | 251.199 | 3 |

实验集预测完整性：

```text
预测记录数：5000
预测缺失数：0
预测最小值：0.000000 mm
预测平均值：31.867348 mm
预测最大值：645.139111 mm
```

### 输出路径

```text
problem4_outputs/data/问题4_建模与预测结果.xlsx
problem4_outputs/data/问题4_训练集分阶段建模明细.csv
problem4_outputs/data/问题4_实验集表面位移预测明细.csv
problem4_outputs/tables/问题4_训练集阶段划分.csv
problem4_outputs/tables/问题4_阶段趋势模型参数.csv
problem4_outputs/tables/问题4_模型检验指标.csv
problem4_outputs/tables/问题4_时间分块交叉验证.csv
problem4_outputs/tables/问题4_岭回归标准化系数.csv
problem4_outputs/tables/问题4_因素贡献度.csv
problem4_outputs/tables/问题4_实验集特征截尾记录.csv
problem4_outputs/tables/表4_1_实验集指定时刻预测值.csv
problem4_outputs/logs/问题4_处理日志.txt
problem4_outputs/logs/问题4_处理日志.json
problem4_outputs/figures/问题4_训练集阶段划分图.png
problem4_outputs/figures/问题4_训练集累加拟合图.png
problem4_outputs/figures/问题4_实验集表面位移预测图.png
problem4_outputs/figures/问题4_实验集预测增量与爆破标记图.png
```

### 阶段节点敏感性分析补充

已对训练集两个阶段转换节点进行敏感性分析。扰动方案包括第一节点或第二节点分别提前/滞后12小时、提前/滞后24小时，以及两个节点同步提前/滞后12小时。

主要结论：

```text
第一转换节点单独扰动时，表4.1最大绝对变化约为 5.160357 mm。
第二转换节点单独扰动时，表4.1最大绝对变化约为 68.864161 mm。
两个节点同步扰动12小时时，表4.1最大绝对变化约为 36.492685 mm 或 31.496637 mm。
```

结果表明，问题四预测结果对“缓慢匀速形变 -> 加速形变”转换节点不敏感，但对“加速形变 -> 快速形变”转换节点较敏感。原因在于第二转换节点直接决定训练集中快速阶段样本的起点，而快速阶段位移速度显著高于前两阶段，因此其边界变化会改变快速阶段趋势项和实验集后期预测值。

新增输出路径：

```text
problem4_outputs/tables/问题4_阶段节点敏感性分析.csv
problem4_outputs/figures/问题4_阶段节点敏感性_表4_1变化.png
problem4_outputs/figures/问题4_阶段节点敏感性_训练误差.png
```
## 已完成工作记录：问题五变量组合建模与分阶段预警机制

### 执行状态

问题五已完成第一版建模、变量组合误差比较、最优组合确定、分阶段速度阈值预警机制构建和图表日志输出。原始附件未修改，所有结果统一输出至：

```text
problem5_outputs
```

执行脚本：

```text
problem5_solution.py
```

### 建模口径

附件5共 9450 条记录，时间范围为 2024-01-01 16:40 至 2024-03-07 07:30，采样间隔为 10 分钟。除爆破点距离和单段最大药量外，其余核心变量无缺失；爆破变量空值解释为非爆破时刻，不作为普通缺失值插补。

问题五采用“阶段识别—变量组合比较—速度阈值预警”的建模路径。首先基于表面位移稳健趋势识别三段式形变阶段；随后对六类候选变量进行“每次剔除一类”的 6 选 5 组合比较；最后基于平滑位移速度分布设置正常、关注、警戒和危险四级预警阈值。

外推风险检查说明：附件5为单一完整训练序列，不含独立实验集预测任务；当前模型评价采用阶段内时间分块交叉验证，避免随机划分破坏时序结构。由于第三阶段样本数相对较少，模型在快速形变阶段以样本内解释和阶段内验证为主，不宜将所得阈值直接外推至其他边坡或其他监测点。

实验集初始位移基准说明：问题五不包含实验集，也不需要对未知表面位移序列进行累计预测，因此不存在实验集初始位移基准设定。模型使用附件5已观测表面位移作为阶段识别、误差评估和速度阈值构建依据。

### 主要结果

阶段划分结果：

| 阶段 | 时间范围 | 平均速度/mm/h |
|---:|---|---:|
| 1 | 2024-01-01 16:40 至 2024-02-03 02:40 | 1.547527 |
| 2 | 2024-02-03 02:50 至 2024-03-03 01:50 | 2.018909 |
| 3 | 2024-03-03 02:00 至 2024-03-07 07:30 | 9.341113 |

最优五变量组合：

```text
纳入变量：降雨量、孔隙水压力、微震事件数、爆破点距离、单段最大药量
剔除变量：干湿入渗系数
```

最优组合模型误差：

```text
训练 RMSE：57.521365 mm
训练 MAE ：44.490787 mm
训练 R2  ：0.996523
阶段内时间分块交叉验证 RMSE：192.178961 mm
阶段内时间分块交叉验证 MAE ：126.247172 mm
阶段内时间分块交叉验证 R2  ：0.961193
```

预警阈值：

| 预警等级 | 速度条件 |
|---|---|
| 正常 | v < 1.794 mm/h |
| 关注 | 1.794 <= v < 5.376 mm/h |
| 警戒 | 5.376 <= v < 9.270 mm/h |
| 危险 | v >= 9.270 mm/h |

关注及以上等级要求连续不少于 6 个采样点满足速度条件，即持续约 1 小时。

### 输出路径

```text
problem5_solution.py
problem5_outputs/data/问题5_建模结果.xlsx
problem5_outputs/data/问题5_建模明细.csv
problem5_outputs/data/问题5_预警等级明细.csv
problem5_outputs/tables/问题5_变量组合误差比较.csv
problem5_outputs/tables/问题5_最优变量组合模型参数.csv
problem5_outputs/tables/问题5_最优模型因素贡献度.csv
problem5_outputs/tables/问题5_阶段划分与平均速度.csv
problem5_outputs/tables/问题5_预警等级统计.csv
problem5_outputs/tables/问题5_预警阈值表.csv
problem5_outputs/logs/问题5_处理日志.txt
problem5_outputs/logs/问题5_处理日志.json
problem5_outputs/figures/问题5_阶段划分图.png
problem5_outputs/figures/问题5_变量组合误差比较.png
problem5_outputs/figures/问题5_速度阈值预警图.png
```

### 后续优化结果

已补充“仅阶段趋势基准模型”与“阶段趋势+最优五变量模型”的对照。结果表明，仅阶段趋势模型的阶段内时间分块交叉验证 RMSE 为 186.361186 mm，阶段趋势+最优五变量模型的阶段内时间分块交叉验证 RMSE 为 192.178961 mm。五变量模型训练误差略低，但交叉验证误差未优于仅阶段趋势基准模型，说明附件5表面位移主要由阶段演化趋势解释，外部变量对跨时间块预测的稳定增益有限。论文中应将“最优五变量组合”限定为六种五变量组合内部的最优结果，不应表述为优于阶段趋势基准。

已补充预警持续性判据敏感性分析，比较连续 3、6、12 个采样点触发条件下的预警等级分布：

| 连续采样点数 | 持续时间/h | 关注及以上样本数 | 警戒及以上样本数 | 危险样本数 |
|---:|---:|---:|---:|---:|
| 3 | 0.5 | 1555 | 896 | 183 |
| 6 | 1.0 | 1174 | 743 | 27 |
| 12 | 2.0 | 910 | 505 | 0 |

敏感性结果表明，持续性约束越严格，预警触发次数越少，危险等级尤其敏感。当前主方案采用连续 6 个采样点，即约 1 小时，作为误报控制和及时性之间的折中口径。

新增输出路径：

```text
problem5_outputs/tables/问题5_阶段趋势基准模型对比.csv
problem5_outputs/tables/问题5_预警阈值敏感性分析.csv
problem5_outputs/figures/问题5_阶段趋势基准模型对比.png
problem5_outputs/figures/问题5_预警阈值敏感性分析.png
```
